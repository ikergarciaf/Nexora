import { describe, it, expect } from 'vitest';
import React from 'react';

// Placeholder: component tests will be expanded
describe('Odontogram', () => {
  it('renders without crashing', () => {
    expect(true).toBe(true);
  });
});
